# port number
server:
  port: 8081

#service name
spring:
  application:
    name: service-edu
  #environment: dev, test, prod
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/guli?serverTimezone=UTC
    username: root
    password: foreveryoung1
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT-7

#mybatis logs
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



